# DATA SPEAKS!! Dashboard

A static, Vercel-ready dashboard for the Student Performance ML project.

The dashboard is built with HTML/CSS/JavaScript and uses the generated ML figures. No backend or build step is required.
